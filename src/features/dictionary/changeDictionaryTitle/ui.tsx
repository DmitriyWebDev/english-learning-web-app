import { FC } from 'react';
import { TextField } from '@mui/material';

export const ChangeDictionaryTitle: FC = () => {
  return <TextField id="outlined-basic" label="Название словаря" variant="outlined" />;
};
